# Postgres client for db.py — everything else is still stdlib only.
psycopg2-binary
# Used by main.py's get_vehicle_reference_image() to compress the vehicle
# base reference to JPEG (already wrapped in a try/except with a PNG
# fallback if this is ever missing, but install it for the smaller/faster
# reference image in production).
Pillow
